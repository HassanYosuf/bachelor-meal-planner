[build]
  command = "sed -i 's|SUPABASE_ANON_KEY_PLACEHOLDER|'\"$SUPABASE_ANON_KEY\"'|g' app.js"
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
